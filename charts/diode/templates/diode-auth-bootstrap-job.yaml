{{- if .Values.diodeAuthBootstrap.enabled -}}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "diode.auth.bootstrapjobname" . }}
  namespace: {{ include "diode.namespace" . }}
  labels:
    {{- include "diode.labels" . | nindent 4 }}
    app.kubernetes.io/component: {{ include "diode.auth.bootstrapjobname" . }}
  {{- if or .Values.global.commonAnnotations .Values.diodeAuthBootstrap.job.annotations }}
  annotations:
    {{- if .Values.diodeAuthBootstrap.job.annotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.diodeAuthBootstrap.job.annotations "context" $ ) | nindent 4 }}
    {{- end }}
    {{- if .Values.global.commonAnnotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.global.commonAnnotations "context" $ ) | nindent 4 }}
    {{- end }}
  {{-  end }}
spec:
  backoffLimit: {{ .Values.diodeAuthBootstrap.job.backoffLimit }}
  template:
    metadata:
      labels:
        {{- include "diode.selectorlabels" . | nindent 8 }}
        app.kubernetes.io/component: {{ include "diode.auth.bootstrapjobname" . }}
    spec:
      serviceAccountName: {{ include "diode.auth.serviceaccountname" . }}
      {{- if .Values.diodeAuthBootstrap.image.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.diodeAuthBootstrap.image.imagePullSecrets | nindent 8 }}
      {{- end }}
      initContainers:
        {{- if .Values.hydra.enabled }}
        - name: wait-for-oauth2-server
          image: {{ include "diode.busybox.image" . }}
          imagePullPolicy: {{ include "diode.busybox.imagepullpolicy" . }}
          command:
            - sh
            - -c
            - |
              host={{ include "diode.hydra.public.hostname" . }}
              port={{ include "diode.hydra.public.port" . }}
              until nc -z $host $port; do
                echo "Waiting for OAuth2 server";
                sleep 3;
              done;
              echo "OAuth2 server is up and running";
        {{- end }}
        {{- if .Values.diodeAuthBootstrap.job.extraInitContainers }}
        {{- include "common.tplvalues.render" ( dict "value" .Values.diodeAuthBootstrap.job.extraInitContainers "context" $ ) | nindent 8 }}
        {{- end }}
      containers:
        - name: {{ include "diode.auth.bootstrapjobname" . }}
          image: "{{ .Values.diodeAuthBootstrap.image.repository }}:{{ .Values.diodeAuthBootstrap.image.tag }}"
          imagePullPolicy: {{ .Values.diodeAuthBootstrap.image.pullPolicy | default "IfNotPresent" }}
          command: ["/bin/sh", "/etc/config/oauth2/bootstrap-clients.sh"]
          env:
            - name: HYDRA_ADMIN_URL
              value: {{ include "diode.hydra.admin.url" . }}
          volumeMounts:
            - name: oauth2-secret
              mountPath: /etc/config/oauth2/client
              readOnly: true
      volumes:
        - name: oauth2-secret
          secret:
            secretName: {{ include "diode.auth.oauth2.secret" . }}
      restartPolicy: OnFailure
{{- end }} 
